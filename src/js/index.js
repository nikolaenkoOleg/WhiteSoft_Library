import '../sass/main.sass';
import app from '../app/index.jsx';

// const baseUrl = process.env.NODE_ENV === 'production' ? 'http//your-url' : 'http://localhost:7700';

// console.log(axios.get(`${baseUrl}/src/books.json`).then((response) => response.data));

app();
